# /etc/ssh/sshd_config.d/50-hardening.conf — key-only auth for all users.
# Install as root:  cp ~/.dotfiles/sshd/50-hardening.conf /etc/ssh/sshd_config.d/
#                   systemctl reload sshd
PasswordAuthentication no
KbdInteractiveAuthentication no
